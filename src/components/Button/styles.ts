import { StyleSheet } from "react-native";

export default StyleSheet.create({
    button: {
        borderRadius: 8,
        padding: 8,
        backgroundColor: 'indigo',
    },
    buttonText: {
        color: 'white',
        fontWeight: 300,
    },
});
